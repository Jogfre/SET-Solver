<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SETSolver" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/bkg_primary</item>
        <item name="android:windowSplashScreenBackground">@color/bkg_primary</item>

    </style>

    <style name="Theme.SETSolver" parent="Base.Theme.SETSolver" />
    <style name="Theme.SETSolver.Fullscreen" parent="Theme.SETSolver">
        <item name="android:actionBarStyle">@style/Widget.Theme.SETSolver.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.SETSolver.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>